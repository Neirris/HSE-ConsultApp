{
  "version": 2,
  "builds": [
    { "src": "server/server.mjs", "use": "@vercel/node" },
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    {
      "src": "/(auth|profile|users|chats|events|notifications|admin)/(.*)",
      "dest": "/server/server.mjs"
    },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
